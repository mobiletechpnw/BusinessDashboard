{
  "name": "business-dashboard",
  "version": "1.0.0",
  "description": "Vault & Pine Collective — Business Command Center (static PWA)",
  "private": true,
  "scripts": {
    "test": "node --test",
    "test:e2e": "node tests/e2e-pages.js",
    "format": "prettier --write \"{*,tests/*}.{js,json,css}\"",
    "format:check": "prettier --check \"{*,tests/*}.{js,json,css}\""
  },
  "devDependencies": {
    "playwright": "1.56.1",
    "prettier": "^3.3.0"
  },
  "dependencies": {
    "@hyperframes/studio": "^0.7.60"
  }
}
